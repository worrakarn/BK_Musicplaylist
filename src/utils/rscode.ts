const rscode = {
  internal_server_error: 'internal-server-error',
  invalid_data: 'invalid-data',
  missing_token: 'missing-token',
  wrong_token: 'worng-token',
  success: 'success',
  no_data: 'no-data',
}

export const { internal_server_error, invalid_data, missing_token, wrong_token, success, no_data } = rscode
